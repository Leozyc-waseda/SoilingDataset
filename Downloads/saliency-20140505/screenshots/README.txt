To run the demos, do the following from the top-level directory:

  ./configure             # if not done previously
  make core               # if not done previously

Then cd to this directory:

  cd screenshots

and build the demo runner script:

  ./build.tcl runner

and then run the demo runner script:

  ./rundemo.sh
